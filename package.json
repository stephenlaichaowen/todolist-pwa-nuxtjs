{
  "name": "cool-todo-list",
  "version": "1.0.0",
  "description": "My divine Nuxt.js project",
  "author": "stephenlai",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "github": "git add . && git commit -m 'commit' && git push origin master",
    "deploy": "npm run generate && npm run github"
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "@nuxtjs/pwa": "^3.0.0-0"
  },
  "devDependencies": {
    "eslint-config-prettier": "^6.10.0",
    "eslint-plugin-prettier": "^3.1.2",
    "prettier": "^1.19.1"
  }
}
